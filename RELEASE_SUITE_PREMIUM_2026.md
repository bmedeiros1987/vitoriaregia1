# Release — Suite Premium 2026

Entrega preparada em 17 de julho de 2026.

- Branch de segurança antes da publicação: `backup/pre-suite-premium-2026-07-17`.
- Branch de implementação: `agent/suite-premium-completa`.
- Implementação isolada em arquivos públicos estáticos e carregamento no `client/index.html`.
- Nenhuma migração de banco ou alteração das regras centrais nesta entrega.
- Scripts validados com `node --check`.

Para publicar no Render após o merge, execute **Deploy latest commit**. Se os arquivos visuais antigos permanecerem em cache, utilize **Clear build cache & deploy** e atualize o navegador com recarregamento completo.
