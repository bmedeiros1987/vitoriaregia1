{
  "name": "vitoria-regia-pro",
  "version": "10.3.0",
  "private": true,
  "description": "Sistema Vitória Régia Pro v10.2 com layout premium redesenhado, foto real do prédio, menu expansível e mobile-first",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run dev",
    "build": "npm --prefix client run build && npm --prefix server run build && rm -rf server/public && mkdir -p server/public && cp -a client/dist/. server/public/",
    "start": "npm --prefix server start",
    "postinstall": "npm --prefix client install --no-audit --no-fund && npm --prefix server install --omit=dev --no-audit --no-fund",
    "install:all": "npm install --no-audit --no-fund"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "20.19.0"
  }
}
